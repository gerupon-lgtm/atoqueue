# テンパリスト起動URL連携 実機確認記録票

作成日: 2026-09-11。対象要件: F-020。

## 1. 確認の境界

- 本番向けUIは「タスク → チェックリストにする → 合成Task2件を選択 → 内容を確認 → 内容を確定 → テンパリストで開く」。ローカル保存後に `location.assign` で遷移する。実機ではこの経路を使い、実際の利用者Task本文は使わない。
- `/dev/tempalist-link` は開発時だけの先行試験画面。固定UUID・固定2項目の通常anchorで比較できるが、本番ビルドには含まれない。製品UIの保存後起動やOS動作をこの試験画面だけで確認済みにしない。
- 受信側で作成前確認が表示されることを確認し、明示操作なしに保存しない。
- HTTPS試験配置、本番設定変更、実データ使用は利用者の承認後に行う。
- `target` 指定やURL起動だけでインストール済みPWAが開くことは保証しない。作成先の保存領域と操作性を実機で確認する。

## 2. 事前記録

| 確認日時 | 担当 | あとキュー版 | テンパリスト版 | 端末 | OS版 | ブラウザ版 | 両PWAインストール状態 |
| ---------- | ---- | ---------------- | -------------------- | ---- | ---- | ------------ | -------------------------- |
| 未実施     |      |                  | 0.4.0                |      |      |              |                            |

## 3. 実機マトリクス

| No. | OS | 送信側PWA起動状態 | 受信側PWA起動状態 | URLの開き方 | 作成前確認の表示 | 保存後、普段のPWAから同じリストを再表示 | 同一URL再起動時の重複防止 | 結果・起動先・操作数メモ |
| --- | -- | ------------------- | ------------------- | ------------ | ------------------ | ----------------------------------------- | ----------------------------- | ---------------------------- |
| 1 | Android | 起動中 | 起動中 | 「テンパリストで開く」をタップ | 未実施 | 未実施 | 未実施 | |
| 2 | Android | 起動中 | 終了済み | 「テンパリストで開く」をタップ | 未実施 | 未実施 | 未実施 | |
| 3 | Android | ブラウザタブ | 起動中 | 「テンパリストで開く」をタップ | 未実施 | 未実施 | 未実施 | |
| 4 | Android | ブラウザタブ | 終了済み | 「テンパリストで開く」をタップ | 未実施 | 未実施 | 未実施 | |
| 5 | iOS/iPadOS | 起動中 | 起動中 | 「テンパリストで開く」をタップ | 未実施 | 未実施 | 未実施 | |
| 6 | iOS/iPadOS | 起動中 | 終了済み | 「テンパリストで開く」をタップ | 未実施 | 未実施 | 未実施 | |
| 7 | iOS/iPadOS | Safariタブ | 起動中 | 「テンパリストで開く」をタップ | 未実施 | 未実施 | 未実施 | |
| 8 | iOS/iPadOS | Safariタブ | 終了済み | 「テンパリストで開く」をタップ | 未実施 | 未実施 | 未実施 | |

## 4. 判定欄

| 判定対象 | 結果 | 根拠 | 次の対応 |
| -------- | ---- | ---- | ---------- |
| Androidの正式起動方式 | 未確定 | 実機未実施 | 上記No. 1〜4を確認 |
| iOS/iPadOS対応可否 | 未確定 | 実機未実施 | 上記No. 5〜8の保存領域と操作数で判定 |
| 公開可否 | 未確定 | HTTPS試験配置は未承認・未実施 | 利用者承認後に配置と実機確認 |

本番向けUIの判定は上記の「テンパリストで開く」ボタンから保存後に `location.assign` する方式で行い、試験した方式を結果欄にも記す。DEVの通常リンクと比較した場合は別の試験として記録する。すべての未実施欄は実機証跡を得るまで変更しない。

## 5. ローカル実装と実機で残る確認

- ローカル実装版: `mvp-1.28.0`。端末保存schemaVersionは11、起動URLのschemaVersionは1。正式アイコン待ちのため文字ラベル「テンパリスト連携済」を使用する。
- 自動試験: 合成データをブラウザ内で受け止める。URLフラグメントを含む `page.url()` から契約を検査し、外部サービスでは保存しない。詳細な品質ゲートは [リリース記録](releases/mvp-1.28.0.md) を参照。
- Android: 両PWA起動中／受信側終了時、起動先、保存後に普段のテンパリストをホーム画面から開いた時の同一リスト、同じURLでの再試行による既存リスト再利用を確認する。
- iOS/iPadOS: 同じ条件で保存領域と操作数を記録する。成立しない場合は本連携のみAndroid限定とする判断を行う。現在は未判定であり対応済みとは案内しない。
- 相手側に未保存入力がある場合の中断と再試行、あとキューへ戻った際の表示も記録する。「連携済」は開く操作の受付履歴であり、受信・保存成功を保証しない。
- 320px表示・キーボード操作のブラウザ試験はOSのPWA捕捉・ソフトキーボード・保存領域の実機証跡にはならない。
- 状態は「ローカル自動検証」「実機確認待ち」「アイコン待ち」「本番未公開」を分けて管理する。本作業でHTTPS試験配置、本番deploy、API再起動、テンパリストrepoへの書込は行っていない。
