# mvp-1.20.0 検証記録

確認日: 2026-08-31

対象要件: F-002、F-012、NF-001、NF-008、NF-009

## 変更範囲

- 承認済み画像に合わせ、共通ヘッダ行・期限超過案内の見た目を28px、ヘッダ下の余白を4pxに変更。
- 「あとキュー」は元の位置から右へ4px、右揃えの期限超過案内は左へ4px。文字サイズ、Kosugi、色、太さ、字間は変更しない。
- 期限超過案内がない場合もヘッダ行の高さを維持。案内の押下領域だけを上側の既存余白へ16px広げ、本文へ重ねず44pxを確保。
- 保存形式、通知設定・予約、今日の確認の履歴保持ルールは変更なし。
- 複数画面共通の変更として、表示・Web・API・パッケージを `mvp-1.20.0` / `1.20.0` に揃える。

## 検証

- RED: 変更前の実ブラウザで追加7ケースの失敗を確認。表示ありでは44px、表示なしでは約15.6px、下余白6px、左右移動0pxで指定と不一致。上部余白からの押下も遷移しないことを確認した。
- GREEN: 全E2E48件成功（`--workers=1 --trace retain-on-failure`）。320×760、360×450、412×760、1280×760の表示あり／なしでヘッダ28px・下余白4px・左右4pxを確認。412pxでは見出し1行を確認。
- 320×640、360×450、412×760の入力画面が期限超過案内追加前（mvp-1.17.1）よりページ高を増やさないこと、完了・アーカイブ後に0件となってもヘッダ位置・高さを維持すること、上部に広げた押下範囲から期限超過一覧へ移動できることを確認。
- 単体・結合: 61ファイル531件成功。
- lint、全ワークスペース型検査・build、配置成果物検査: 成功。最終テスト補強後の対象ファイルlintも成功。
- 独立した読み取り専用レビュー: Critical・Importantなし。Minorの旧版スクロール比較基準を実際の旧版ヘッダ寸法へ修正し、再レビューで解消を確認。
- 320pxの実ブラウザ画像も目視確認。文字色・書体・操作色・下部ナビを維持し、入力欄と保存操作が画面内に収まる。

E2E実行中の一時traceファイルをlintが拾い、最初の並行実行だけ失敗した。アプリのコードは変更せず、E2E終了後にlintを再実行して成功した。以後、全体lintとE2Eの一時成果物生成は同時実行しない。Web buildもE2E実行中には重ねない。

Pixel実機の表示は未確認。ブラウザ検証にはテスト専用データを使用し、利用者の端末データは変更しない。

## 公開状況

未配置。品質ゲート完了後、公開結果とリリースコミットを追記する。
