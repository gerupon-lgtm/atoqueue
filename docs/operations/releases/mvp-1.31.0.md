# mvp-1.31.0 !=テンパリスト正式名と連携操作のサイズ調整

対象: F-020 / NF-008 / NF-009。承認画像 `dist/preview-wide-tempalist-height-40.png` と、直前の連携画面のⓘ・戻るボタンを同じサイズ感にする依頼に対応。実装・全体検証完了、未公開。

## 承認仕様と実装方針

- 正式表示名は `!=テンパリスト`。一覧の主操作は `!=テンパリストへ`。ヘルプ・起動操作・連携済の読み上げ名等、送信側の表示を揃える。URL・内部識別子・保存キー・連携契約は変更しない。
- 一覧は主操作と再確認の幅を16:13とし、390px画面では160px／130px、高さ40pxにする。主操作左端39px・両隙間6px・ⓘ中心350px・下の絞り込みまで12pxは維持。狭幅の折り返しは切り捨てず等高を保つ。
- 「直前の連携」画面だけ、ⓘを一覧と同じ18pxの控えめな表示（透明背景・枠なし）にする。戻るボタンは幅130px・通常高さ40px・左寄せ、従来の色を維持。長い文言の再起動ボタンや、選択内容の確認画面の操作サイズは変更しない。
- タップ範囲は縦44px以上。上下の透明領域が近隣のボタンやカードと重ならないようにし、無効状態・キーボード・説明開閉・戻る処理を維持する。
- 配色・フォント・カード高・右端バッジを維持。共通の説明文は正式名へ修正するが、通知・保存・起動ロジックは変更しない。
- 複数画面の変更として1.31.0へマイナー更新。未コミットの利用者所有3文書と未追跡ファイルは対象外。レビュー基点は6385081。

## 検証計画

1. 正式名・幅配分・40px高さ・遷移先の小さいⓘと戻るボタンをブラウザテストへ追加してREDを確認。
2. 最小の表示変更後、320/360/390/414/1024px・上下の押下領域・説明開閉・戻る・iOS制限を確認。
3. 単体・結合・E2Eは5ファイル以下ずつ1ワーカーで実行し、型検査・lint・全buildと2軸レビューを記録する。
4. 公開する場合は検証済みSHAを指定し、既存Deployのtarget=pwaのみ使用。通知API・本番DBは変更しない。

## 実装・検証記録

- 正式名への変更前は `!=テンパリストへ` の操作が存在せずfocused E2Eが失敗。表示・aria・title・送信側エラー文を訂正後、幅比16:13／高さ40pxと遷移先のサイズ・上下押下領域を含む同じテストが成功。
- 320/360/390/414/1024pxで一覧、320/390/414pxで直前の連携画面を検証。390px画像は `dist/tempalist-1310-focused/tempalist-transfer-F-020-c-bef27-om-actions-at-mobile-widths/{entry,retry}-390.png`。PCブラウザの実装画像であり、OS実機画像・生成画像ではない。
- 連携済バッジのツールチップは `!=テンパリストへ開く操作済み（受信・保存の成功ではありません）`。読み上げ用ラベル、ヘルプ見出し・本文、iOS案内も正式名に統一。内部識別子・URL・保存形式は変更していない。
- 全単体・結合80ファイル757件、18バッチすべて終了0（`dist/tempalist-1310/unit`）。型検査・lint・build・全E2Eは下へ追記する。
- 6385081基点の2軸レビュー: Spec指摘0、Standardsは文書追補の同期確認後に指摘0。要件・画面・基本設計・MVP計画へ1.31.0の差分を反映した。
- 今回は実装確認までとし、push・デプロイは行っていない。公開版は1.30.1のまま。
- 最終E2E17ファイル61件、4バッチすべて終了0。全5workspace型検査・全体lint・正式build・配置成果物検査・配置補助2件も終了0（`dist/tempalist-1310/{e2e,static}`）。単体・結合と合わせ再試行なしで成功。既存のVitest workspace非推奨・500kB bundle・inlineDynamicImports非推奨警告は継続。
- 最終画像: `dist/tempalist-1310/e2e/batch-3/tempalist-transfer-F-020-c-bef27-om-actions-at-mobile-widths/entry-390.png` と `retry-390.png`。一覧と直前の連携画面は主担当が目視確認。ツールチップのtitleは単体テストでも正式名を確認した。
