# mvp-1.20.1 検証記録

確認日: 2026-08-31

対象要件: F-014、F-016、NF-001、NF-009

## 変更範囲

- タスク修正画面の状態カードを、概要の下・内容カードの上へ移動。
- 完了・アーカイブ済みの「再開」と状態操作の結果メッセージも同じ位置へ移動。DOM順とキーボード順を表示順に一致させる。
- カードの書体・配色・サイズ、保存・通知・履歴のロジック、保存形式は変更しない。
- 単一画面の表示変更として表示・Web・API・各パッケージを `mvp-1.20.1` / `1.20.1` へ更新。

## 検証

- RED: 対応中・完了・アーカイブの操作順と後回し結果の位置の計4ケースが失敗することを確認。E2Eでも状態カードが内容より下にある実寸による失敗を確認。
- GREEN: タスク修正画面の18件が成功。対応中・完了・アーカイブの操作順と、状態操作の結果メッセージが内容より上にあることを確認。
- 単体・結合: `corepack.cmd pnpm test --maxWorkers=1 --no-file-parallelism` — 61ファイル・534件成功。
- E2E: `corepack.cmd pnpm --filter @atoqueue/web exec playwright test --workers=1 --trace retain-on-failure` — 48件成功。
- `corepack.cmd pnpm lint`、`corepack.cmd pnpm typecheck`、`corepack.cmd pnpm build`、`node deploy/scripts/verify-deployment-artifacts.mjs` — すべて成功。
- 412×760pxのブラウザ画像で、概要・状態・内容の順序と既存の配色・カード形状を確認。キーボードで「タスク一覧」の次に「完了」へ移り、Enterで完了状態が端末内へ保存されること、処理後の「再開」も内容より上に出ることをE2Eで確認。
- ビルドは成功。既存PWA依存の `inlineDynamicImports` 非推奨警告は継続するが、今回の配置変更によるものではない。
- 独立した読み取り専用レビュー: 指摘なし。JSXの移動のみでハンドラやスタイルを変えていないこと、仕様・版情報・回帰テストの整合を確認。
- `git diff --check` — 成功。

## 公開状況

利用者から本番デプロイの依頼を受領。`mvp-1.20.1` の配置準備中。公開確認が完了するまでは未配置として扱う。
