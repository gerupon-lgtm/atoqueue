# mvp-1.21.0 検証記録

確認日: 2026-08-31

対象要件: F-009、F-010、F-012、NF-004、NF-010

## 変更範囲

- 今日の確認の手動前後移動を、当日の確認リスト全体（完了・アーカイブを含む）の閲覧へ変更。タスク画面から処理済みにしたTaskも同じ位置で表示する。
- ページ番号・カード・操作対象を一致させ、外部画面で処理済みの状態別案内と「現在：」も表示する。
- 回答直後の未回答優先、完了位置からの起動時再開、翌日の再計算は維持。確認開始前の完了Taskを新たに追加せず、全回答後は従来の結果画面へ進む。
- 保存形式・通知API・期限計算・書体・配色・ヘッダ寸法は変更しない。ロジック変更のため `mvp-1.21.0` / `1.21.0` とする。

## 原因

手動の前へ移動は保存位置だけを戻す一方、カード取得は確認内の回答記録がない完了項目を飛ばして次の対応中Taskを返していた。番号は保存位置を示すため不一致になった。回答処理にも同じ飛び越しがあり、履歴表示だけを直すと別Taskを操作するため、選択・表示・操作対象を統一した。

## 検証

- RED: ドメイン・画面の対象62件中7件が不具合を検出。別画面で処理済みのカード表示、前後の巡回、選択項目の欠落時に別Taskを代用しないこと、再回答対象を検証。
- 追加RED: 過去に完了・アーカイブと回答したTaskを別画面で再開した場合、古い回答ではなく現在の対応中状態を表示する2件が失敗。その修正後、対象64件が成功。
- GREEN: 新規E2Eも実際のタスク画面で3件を処理し、4枚すべての閲覧・状態表示・再起動・再回答が成功。
- 全単体・結合: `corepack.cmd pnpm test --maxWorkers=1 --no-file-parallelism` — 61ファイル・541件成功。
- 全E2E: `corepack.cmd pnpm --filter @atoqueue/web exec playwright test --workers=1 --trace retain-on-failure` — 49件成功。
- `corepack.cmd pnpm lint`、`corepack.cmd pnpm typecheck`、`corepack.cmd pnpm build`、`node deploy/scripts/verify-deployment-artifacts.mjs`、`git diff --check` — 成功。
- 独立レビュー: Critical / Important 0件。任意のテスト強化提案を採用し、閲覧によってTask・通知予約・通知対応表・回答済みID・確認所有イベントIDが変わらない検証を追加。対象画面17件と型検査を再実行し成功。
- 元の再現操作を修正ビルドでも実行: 412×760px、4件中3件を実際のタスク画面で完了。起動時は豆腐 `2 / 4`、前へ戻ると完了確認用1 `1 / 4`（「このタスクは完了マーク済みです。」「現在：完了」）、次へ進むと豆腐 `2 / 4`。旧版で再現した番号と内容の不一致が解消。ローカルの隔離データを使用し、本番データ・通知APIは変更していない。

## 公開状況

- 2026-08-31、本番デプロイの依頼を受領。配置直前に単体・結合541件、E2E49件、lint・型検査・全build・配置成果物検査・CI対象の書式チェックを再実行し、すべて成功。
- リリースコミット: `8f319d1f979663ba7a1fe5cbc9133ce3bc5fa0e5`（`task/atoqueue-mvp`）。
- [本番デプロイ Run 33380593530](https://github.com/gerupon-lgtm/atoqueue/actions/runs/33380593530) は `success`。品質ゲート・PWA公開・通知API配置の全ジョブが成功。
- 同じコミットのpushは Deploy `33380578582` / CI `33380578594`、PRは Deploy `33380581583` / CI `33380581641` がすべて成功。
- 2026-08-31 19:06 JST、公開PWAはHTTP 200。画面の版と通知API `/healthz` の版が `mvp-1.21.0`、APIは `status: "ok"` を返す。
- 公開JS `/assets/index-CaXtpXSN.js` のSHA-256は `c9df6e6eec5028ba737917d2f97df673ee2348caca5b7aca0b1b6818795d7a91`、CSS `/assets/index-C4t6XpMj.css` は `7a9fbc2182d7163dcce425c2c11b6c6722b4bc9757d35419eeeb916898692aa1`。両方とも検証済みローカルビルドと一致。
- 公開画面を412×760pxで操作。タスク画面から3件を完了・アーカイブした後、今日の確認で病院 `1 / 4`（完了）、豆腐 `2 / 4`（対応中）、整髪ポマード `3 / 4`（アーカイブ）、ペットボトルを捨てにいく `4 / 4`（完了）が順に表示され、最後から先頭へ戻れることを確認。
- 完了・アーカイブそれぞれの案内文と「現在：」の状態表示を確認。画像で既存の緑・赤の強調も目視確認。閲覧だけではTask・回答履歴・通知予約・通知対応表が変わらないこと、再起動時に豆腐 `2 / 4` へ戻ること、前へ戻って再回答した場合は表示中の病院だけが更新されること、続けて豆腐を完了すると結果画面へ進むことも成功。
- 確認は独立ブラウザの確認用データだけを使用。通知APIへのブラウザ通信は遮断し、利用者のデータ・通知予約は変更していない。Pixel実機での確認は未実施。
- GitHub Actions依存のNode.js 20非推奨注記とPWA依存の `inlineDynamicImports` 非推奨警告は出るが、ジョブの失敗はない。今回の変更で依存更新は行っていない。

端末ではアプリを開き直し、「改行で登録」下が `mvp-1.21.0` になったことを確認する。データ削除・再インストールは不要。
