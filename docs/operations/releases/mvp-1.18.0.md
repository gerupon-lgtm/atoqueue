# mvp-1.18.0 検証記録

確認日: 2026-08-31

対象要件: F-002、F-003、F-009、F-012、F-016、NF-001、NF-009、NF-010、NF-013

## 変更範囲

- 全画面の小さなアプリ名と同じ行に「期限超過 N件 確認」を表示し、期限超過一覧への入口を共通化。既存の緑・生成り・Kosugiを維持し、時計アイコンと橙色の補助色を追加。
- 共通ナビの件数、タスク一覧・詳細・今日の確認の期限表示を同じ判定と補助色に統一。
- 対応中で期限を経過したタスクだけを数える。「今回は閉じる」は含み、完了・アーカイブ・期限なし・未来への期限変更は除外。
- 端末保存成功後、前面復帰時、表示中30秒ごとに件数を更新。通知同期を待たず、保存失敗時は件数を減らさない。
- 記録画面は既存の余白を調整し、案内追加によるスクロール増加を抑制。
- 保存スキーマ、通知API、通知頻度・予約、今日の確認の履歴保持ルールは変更なし。

## 品質ゲート

| 確認 | 結果 |
| --- | --- |
| `pnpm test --maxWorkers=1 --no-file-parallelism` | 61ファイル・531件成功 |
| `pnpm --filter @atoqueue/web exec playwright test --workers=1` | 39件成功 |
| `pnpm lint` | 成功 |
| `pnpm typecheck` | 4ワークスペース成功 |
| `pnpm build` | Web・API・domain・contracts成功 |
| `node deploy/scripts/verify-deployment-artifacts.mjs` | 成功 |

期限超過判定と共通案内の回帰テストは、実装前に失敗を確認してから実装した。既存の通知本文非送信・保存失敗時の復旧・オフライン保持のテストも成功。

独立した読み取り専用コードレビューも実施し、Critical・Important・Minorの指摘はいずれも0件、デプロイ可能と判定した。

## UI確認

- Chromiumの320×640、360×450、412×760で入力・保存・未整理件数更新を確認。変更前相当の見出し・件数余白と比較し、ページ高が増えず、横スクロールも発生しない。
- 通常高では保存ボタンが下部ナビに隠れず、追加スクロールなし。360×450はキーボードで表示領域が低くなった場合の代替検証であり、実機のキーボード操作そのものを検証したものではない。
- 100件表示時の共通案内とナビの `99+` を確認。記録・一覧・詳細・今日の確認のWCAG 2 A/AA・2.1 AA自動検査は違反0件。
- 完了後に「前のタスク」へ戻った履歴カードには期限超過の強調を出さない。全件処理後は共通案内とナビ件数を消す。
- 別タブの保存、前面復帰、時刻経過、バックアップ復元後の件数更新と、検索・カテゴリ条件をリセットした期限超過一覧への遷移を自動検査した。

端末内の利用者データを使わず、テスト専用データで確認した。Pixel実機での今回のUI確認は未実施。

## 公開状況

- リリースコミット: `1514fededbbee1164fee499642f33c34f4fe8888`（`task/atoqueue-mvp`）。
- [本番デプロイ Run 33344036785](https://github.com/gerupon-lgtm/atoqueue/actions/runs/33344036785) は `success`。品質ゲート、GitHub Pages公開、OCI通知API配置の3ジョブがすべて成功。
- 2026-08-31 09:18 JST、公開PWAのトップページと `/assets/index-DD1r5CIq.js` がHTTP 200で取得でき、配信JavaScriptに `mvp-1.18.0` が含まれることを確認。アセット名はローカルでE2E検証したビルドと一致。
- 同時刻、通知API `/healthz` が `status: "ok"`、`version: "mvp-1.18.0"` を返すことを確認。
- GitHub Actionsには利用ActionのNode.js 20指定に関する非推奨警告があるが、Node.js 24で実行され全ジョブは成功。今回のリリースでCI構成は変更していない。

本番アプリ: https://atoqueue.sikumilab.com/

端末側はアプリを開き直し、記録画面の「改行で登録」下で `mvp-1.18.0` を確認する。端末データの削除や再インストールは不要。
