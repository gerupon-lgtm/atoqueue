PORT=
DATABASE_URL=
ALLOWED_ORIGIN=
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=
LOG_LEVEL=
DEADLINE_DELIVERY_LEAD_SECONDS=300
# Keep v2 routes off during the first API-only rollout.
NOTIFICATION_V2_ENABLED=false
# JSON array; keep real per-app VAPID private keys in the server secret file only.
NOTIFICATION_APPLICATIONS_JSON=[]
