# あとキュー（仮称）開発ガイド

## 1. このファイルの役割

このリポジトリで作業するAIエージェント向けの入口である。実装前に次の順で読むこと。

1. `docs/requirements.md`
2. `基本設計サマリ.md`
3. 担当領域に応じて `docs/data-model.md`、`docs/screens.md`、`docs/api-design.md`
4. `docs/tasks.md`
5. `docs/superpowers/plans/2026-08-03-atoqueue-mvp.md`

要件と設計が矛盾する場合は、要件ID `F-*` / `NF-*` を優先し、勝手に解釈を固定せず設計文書へ差分を戻す。

## 2. 実装上の不変条件

- タスク本文、期限、カテゴリ、操作履歴はMVPでは端末内だけに保存する。
- 通知バックエンドへ送ってよいのは端末識別子、通知予約識別子、通知予定時刻、通知種別だけである。
- OS通知の文面は汎用文とし、タスク本文を含めない。
- 期限超過や放置レベルは保存状態ではなく、端末時刻と履歴から再計算する。
- 初回入力では期限やカテゴリを必須にしない。
- タスク候補は自動確定せず、利用者の選択を必要とする。
- 「今日の確認」は1件ずつ表示する。見出しは他画面と同じ左揃えとし、2件目以降だけ右側に矢印なしの `前のタスク` を置く。進行件数はタスクカード内の右上に表示する。
- OS通知の正確な時刻配送は保証しない。アプリ起動時の再計算を必ず併用する。

## 3. 技術方針

以下は実装着手時の【想定】であり、変更時は設計文書と計画書を同時に更新する。

- pnpmワークスペース
- TypeScript / Node.js 24 LTS
- React 19.2 / Vite 8 / PWA
- Fastify 5 / PostgreSQL / Web Push
- Vitest / React Testing Library / Playwright

UI、ドメインルール、保存処理、通知同期を分離する。画面コンポーネントから `localStorage` やHTTP APIを直接呼ばない。

## 4. 品質ゲート

- 変更対象の要件IDをコミットまたは作業メモに明記する。
- ドメインルールは先に失敗するテストを書いてから実装する。
- データ形式を変える場合は `schemaVersion` と移行テストを追加する。
- 通知APIの変更では、サーバーにタスク本文が渡らないことを契約テストで確認する。
- 完了報告前に単体、結合、E2E、型検査、ビルドを実行し、結果を残す。

## 5. 禁止事項

- アカウント、端末間同期、生成AI、外部カレンダー連携をMVPへ無断追加しない。
- OS通知を唯一のリマインド手段にしない。
- 通知許可を初回起動直後に自動要求しない。
- 要件IDを持たない機能を、便利そうという理由だけで追加しない。
