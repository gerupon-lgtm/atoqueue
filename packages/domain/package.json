{
  "name": "@atoqueue/domain",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "pnpm --dir ../.. exec vitest --run --workspace vitest.workspace.ts --project domain",
    "typecheck": "tsc --noEmit",
    "build": "tsc"
  }
}
