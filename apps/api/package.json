{
  "name": "@atoqueue/api",
  "version": "1.17.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --watch --experimental-strip-types src/server.ts",
    "start": "node ./dist/start.js",
    "test": "vitest --run",
    "typecheck": "tsc --noEmit",
    "build": "pnpm --filter @atoqueue/contracts build && tsc && node ./scripts/copy-migrations.mjs"
  },
  "dependencies": {
    "@atoqueue/contracts": "workspace:*",
    "@fastify/cors": "^11.3.0",
    "@fastify/rate-limit": "^11.2.0",
    "argon2": "^0.45.1",
    "fastify": "^5.11.0",
    "pg": "^8.16.3",
    "pino": "^10.3.1",
    "web-push": "^3.6.7",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/pg": "^8.15.5",
    "@types/web-push": "^3.6.4"
  }
}
